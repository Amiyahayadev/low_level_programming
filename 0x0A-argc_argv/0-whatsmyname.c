#include <stdio.h>

/**
 * main - entry point of program
 * @argc: count of all arguments
 * @argv: the string arguments passed from command line
 * Return: 0 on SUCCESs
 */
int main(int __attribute__ ((unused)) argc, char *argv[])
{
	/*PRINT the argument at index sero*/
	printf("%s", argv[0]);
	/*PRINT newline*/
	printf("\n");
	return (0);
}
