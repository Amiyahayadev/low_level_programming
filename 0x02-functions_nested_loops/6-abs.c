#include "main.h"
/**
 * _abs - computer the absolute value of an integer
 * @n: the integer value to
 *
 * Return: n
 */
int _abs(int n)
{
	if (n < 0)
	{
		-(n);
		return (n);
	}
	else
		return (n)
}
